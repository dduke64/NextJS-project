import React from 'react'

const signInPage = ({params}:any) => {
    console.log(params)
  return (
    <div>page shows optional params in the url routing, including the parent with the double bracket</div>
  )
}

export default signInPage